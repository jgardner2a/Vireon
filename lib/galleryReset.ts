/**
 * Legacy one-time local gallery reset — disabled after Supabase migration.
 * Gallery data lives in Supabase Storage + DB only.
 */
export function applyOneTimeGalleryEmptyReset(): void {
  // no-op
}
