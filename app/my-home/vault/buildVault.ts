/** @deprecated Import from `@/lib/evidence/vault` — vault projection lives in lib. */
export {
  buildVaultEntries,
  buildVaultExportBundle,
  enrichFeedsWithAddresses,
  filterFeedsByProperty,
  formatVaultDate,
  groupVaultByProperty,
  projectVaultRelationships,
  vaultSummary,
} from "@/lib/evidence/vault";
